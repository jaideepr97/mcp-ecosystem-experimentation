apiVersion: kuadrant.io/v1
kind: TLSPolicy
metadata:
  name: team-a-gateway-tls
  namespace: team-a
spec:
  targetRef:
    group: gateway.networking.k8s.io
    kind: Gateway
    name: team-a-gateway
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: mcp-ca-issuer
