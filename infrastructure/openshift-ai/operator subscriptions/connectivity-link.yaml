# Red Hat Connectivity Link (includes Kuadrant, Authorino, Limitador operators)
# Manual approval — the install plan must be approved after creation.
---
apiVersion: operators.coreos.com/v1alpha1
kind: Subscription
metadata:
  name: rhcl-operator
  namespace: openshift-operators
spec:
  channel: stable
  installPlanApproval: Manual
  name: rhcl-operator
  source: redhat-operators
  sourceNamespace: openshift-marketplace
