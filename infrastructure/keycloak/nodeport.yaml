apiVersion: v1
kind: Service
metadata:
  name: keycloak-np
  namespace: keycloak
spec:
  type: NodePort
  selector:
    app: keycloak
  ports:
    - name: http
      port: 8002
      targetPort: 8080
      nodePort: 30089
